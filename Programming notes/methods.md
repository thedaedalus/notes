# Methods

- if a method produces a useful value you assign it. i.e

```python
var = object.some_method()
```

- if a method is performing a action you dont need to assign it and just call it on the method i.e

```python
object.some_method(args)
```
